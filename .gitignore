# Env
.env

# Corporate SSL inspection CA — do not commit
netskope-ca.crt
*.crt

# Docker override
docker-compose.override.yml

# Local development binaries downloaded to the repo root (not committed)
/bfg.jar
/composer.phar